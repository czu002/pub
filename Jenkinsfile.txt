node {

    checkout scm

    docker.withRegistry('https://registry.hub.docker.com', 'CZU-docker-HUB') {

        def customImage = docker.build("czu002/dockerwebapp")

        /* Push the container to the custom Registry */
        customImage.push()
    }
}
