node {

    checkout scm

    docker.withRegistry('https://registry.hub.docker.com', 'czu002') {

        def customImage = docker.build("czu/dockerwebapp")

        /* Push the container to the custom Registry */
        customImage.push()
    }
}
